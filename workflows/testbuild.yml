name: Test Build

on:
  pull_request:
    branches: [master]


jobs:
  build_on_pr:
    runs-on: unbuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-versiohn: 22
      - run: npm ci
      - run: npm run build